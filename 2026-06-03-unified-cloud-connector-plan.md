# PJ Cloud Connector — Unified Plan (Dexory S3 + Asensus GCS)

| | |
|---|---|
| **Date** | 2026-06-03 |
| **Status** | Draft for review — user decisions of 2026-06-03 applied (see §8 "Resolved") |
| **Owner** | Davide Faconti |

> **How to read this document.** This plan supersedes nothing; it *unifies* and, in a few enumerated places, *revises* the existing canonical sources: the design spec `2026-05-28-pj-cloud-connector-design.md`, Plan A `2026-05-28-pj-cloud-server-v1.md`, Plan B `2026-05-28-pj-cloud-client-cpp.md`, Plan C `2026-05-28-pj-cloud-integration.md`, and the commercial proposal `2026-06-01-dexory-proposal.md`. Every place this plan *revises* a source is called out inline with **[REVISES …]**. Section 8 separates the decisions already **resolved** (2026-06-03) from those that **still require the user** (mostly commercial) before execution.

---

## 1. Summary — one codebase, two engagements

We will build **one** self-hosted server + **one** PlotJuggler client codebase that serves MCAP recordings from cloud object storage on demand, and use it to deliver **two** paying engagements:

- **Dexory** (AWS S3, self-hosted single Go binary). Commercial frame from the proposal: **Milestone 1** (PoC, EUR 10k, ~1 month) gated on written client approval → **Milestone 2** (hardening + PlotJuggler plugin + WASM browser bonus, EUR 6k, ~1 month).
- **Asensus Surgical** (Google Cloud Storage, deploy on **GCE**). SOW deliverables: cloud connection server, PlotJuggler cloud streaming plugin, GCS auth + file browser, "lazy-loading + prefetch" (satisfied per LOCKED 2 by selection-scoped bulk + **in-memory session reuse**; cross-session/cross-restart persistence is **out of v1** — owned by Asensus's separate "Task C" disk cache or renegotiated — see §3.4 and §8), deployment guide + infra requirements, TLS + compression, metadata search (robot ID / procedure date / operator), MCAP-first with an open path to other formats.

**The thesis.** ~70% of the work — wire protocol, catalog, indexer algorithm, session producer/consumer + retain/resume, dashboard/metrics, the entire Qt `client-core`, and the integration harness — is backend-agnostic *by construction* and is written exactly **once**. It only ever touches two narrow seams (`BlobStore` for storage, `FormatCodec` for recording format), so it cannot know or care whether the bytes come from S3 or GCS, or whether the file is MCAP or something later. The genuine incremental cost of the *second* engagement is **~8–12%**: the GCS `BlobStore` impl, the storage-credential factory split, a `{s3,gcs}` CI matrix dimension, the Asensus GCE deploy guide, and one additive client UI mode (a GCS file-hierarchy browser). Per-deployment behaviour is **config + a small factory layer that selects seam implementations — never a fork.**

This is a deliberately **minimal-diff** evolution of the already-specced Dexory design. The spec had already isolated almost every storage-touching concern behind one narrow interface (`s3reader.RangeGetter`, §8.3) and one indexer list loop (`ListObjectsV2`, §5.2). The wire envelope (§6) carries **zero** storage-specific fields, and `files.s3_key` (§5.1) is already a generic TEXT key that holds `s3://…` or `gs://…` equally. So §6 (wire) and §7 (user flow) are **not edited at all**.

**Two issues the review surfaced are addressed head-on, not papered over:**

1. **Client caching.** The design spec lists client-side disk caching as an explicit **non-goal** (§2 line 40, §12 line 897: "Memory-only via `pj_datastore`; no local disk cache"), and the review confirmed `pj_datastore` has **no disk tier**. **Decision (§8): the v1 client cache is in-memory, session-lifetime reuse only — which *matches* the spec, so this is NOT a reversal and adds no disk-cache engineering.** The cost lands contractually, not technically: Asensus's "repeated access needs no re-download" is honored **only within a running PlotJuggler session**; persistence across restarts/users is out of v1 (their separate "Task C" disk cache or a renegotiated SOW line). See §3.4.
2. **Mosaico reuse data-model mismatch.** "Lift the Mosaico dialog unchanged, swap three seams" collides with a real mismatch: Mosaico keys on a single-string `sequence_name` with per-topic parallel pulls, whereas our session opens **one** multiplexed WS session over a **set** of integer `file_ids[]` with server-side stitching. This plan specifies the reconciliation (§3.3) and downgrades "reused unchanged" to "reused with one defined selection-model adapter."

---

## 2. Locked decisions (restated concisely)

1. **ONE codebase** serves both engagements; per-deployment behaviour is config/backend selection, never a fork.
2. **Streaming = whole-selection eager-bulk only.** The client commits to `(file_ids[], topic_names[], time_range)` and bulk-downloads exactly that selection as fast as network+storage allow. There is **no** viewport-driven random-access prefetch engine. Fast re-open **within a running session** comes from the in-memory client cache (§3.4).
3. **Client cache = in-memory, session-lifetime reuse for BOTH engagements.** Reconstructed `(file, topics, time-range)` data is reused from memory within a running client session so a repeat open avoids re-download. **No disk tier** — this matches design-spec §2/§12 (no reversal). Cross-restart / cross-user persistence is out of v1 (§3.4, §8).
4. **Storage backend = abstraction:** a `BlobStore` interface over HTTP Range GETs, with an S3 impl and a GCS impl.
5. **Auth = abstraction with two separate seams:** (a) client↔server (shared bearer token, pluggable); (b) server↔storage (S3 credentials vs GCS IAM/service-account). Kept strictly independent.
6. **Client UI reuses the Mosaico `toolbox_mosaico` conceptual design** (connect-to-URI + history, browse with metadata, Lua metadata query filter, topic list, time-range slider, multi-topic fetch with progress, settings persistence, datastore ingest). We swap transport (WS + Protobuf) and ingest (raw MCAP records forwarded to PlotJuggler's MessageParser plugins); everything above that line is reusable and overlaps Plan B's `client-core`.

---

## 3. Unified architecture

### 3.1 Shared Go server core — five subsystems, one process, one TCP listener, one TLS cert

Unchanged from spec §3: **Catalog** (WS RPC + SQLite reads, WAL + single catalog-writer goroutine), **Session** (WS streaming + storage fetcher with producer/consumer split for resume), **Indexer** (background storage poller), **Dashboard** (read-only HTML + Prometheus `/metrics` + `/health`). Pure-Go SQLite (`modernc.org/sqlite`, no cgo). All five subsystems consume storage **only** through `BlobStore` and recordings **only** through `FormatCodec`; no subsystem outside `internal/storage` imports a cloud SDK, and only `internal/format/mcap` imports `mcap-go`.

| Subsystem | Change vs spec | Notes |
|---|---|---|
| **Catalog** | Unchanged mechanism. | SQLite-WAL, five tables + `tags_effective` override-wins view. **Load-bearing SHAPE REQ:** `ListFilesResponse` must carry a *flat* `string→string` `metadata` map (the `tags_effective` rows) mapping 1:1 onto Mosaico's `SequenceInfo.user_metadata`. Pin before Phase-1a codegen. `s3_key` renamed *in comment only* to "generic object key/URI". |
| **Indexer** | List call → `BlobStore.List`; footer/summary extraction → `FormatCodec.Extract`. | Algorithm unchanged: poll (default 5 min), change-detect on the `(etag,size,last_modified)` triple. **[REVISES §5.2]** the reader is now the seam, not `s3.ListObjectsV2`. **Metadata source = MCAP footer (resolved, §8):** `robot_id`/`procedure_date`/`operator` are read from each recording's embedded MCAP metadata via `FormatCodec.Extract` → `tags_embedded`. No object-custom-metadata read, no `BlobStore.Head` metadata path, no `ObjectInfo.Metadata` field. |
| **Session** | `RangeGet` → `BlobStore.GetRange`; `IterateMessages` → `FormatCodec.Iterate`. | Mechanism unchanged: `BuildPlan` (overlap validation + chunk intersection + pre-flight estimates + empty-plan contract), producer/consumer split, retain buffer (256 seqs / 64 MB), reconnect-resume (60 s), cancel, whole-selection eager-bulk. **Multi-file stitching and overlap-rejection stay in `session.BuildPlan`, ABOVE the per-file `FormatCodec`** — the codec is per-file (Extract/PlanChunks/Iterate operate on one key) so the "second format = drop-in" claim holds. Batching/compression policy byte-identical (§6.4). |
| **Dashboard + Health/Metrics** | Add a "storage backend + scheme" display line. | Same binary/port/TLS, same pages, Basic auth, `/health`, `/metrics`. |
| **WS + handlers** | Unchanged. | Per-conn read/write loops, priority+bulk channels, dispatcher, `Hello` (version + bearer auth via `ClientAuthenticator`). |

### 3.2 The abstraction seams

Six named seams. Naming `ClientAuthenticator` and `FormatCodec` explicitly (even with one trivial/single impl) is what makes LOCKED 5a's auth separation and Asensus's "MCAP-first, open to other formats" honest at near-zero v1 cost. **Discipline (non-negotiable): build exactly one interface with one impl for `FormatCodec`; do NOT build a plugin system** — a premature plugin abstraction is pure risk for zero v1 value.

| # | Seam | Interface (where it lives) | Dexory / S3 impl | Asensus / GCS impl |
|---|---|---|---|---|
| 1 | **BlobStore** (storage) | `package storage`: `GetRange(ctx,key,off,len)`, `Head(ctx,key) ObjectInfo`, `List(ctx,prefix,token)`. `ObjectInfo{Key,ETag,Size,LastModifiedNs}`. Errors normalized to `ErrTransient` (retry per §8.4 backoff) vs `ErrPermanent` (fail session, `Error{S3_UNAVAILABLE}`). Plus unchanged `io.ReaderAt`/`SectionReader` adapter `OpenReader`, and factory `New(cfg)`. **The only package importing a cloud SDK.** | `s3reader.go` — Plan A Task 13 code retyped to the interface (`GetRange`/`Head` exist; `List` wraps `aws-sdk-go-v2` paginator). 503/429/timeout→Transient; 403/404/NoSuchBucket→Permanent. Keeps the `storage_concurrent_requests` semaphore. | `gcsreader.go` — wraps `cloud.google.com/go/storage`. `GetRange`=`NewRangeReader`; `Head`=`Attrs`; `List`=`Objects`+`iterator.Pager`. **ETag-mapping pin (do before Phase-1b codegen):** the `(etag,size,last_modified)` triple uses GCS `Generation` (monotonic int64) stuffed into the `etag` string + `Updated` as `last_modified`; do **not** use the MD5/CRC32C ETag (unstable for composed/rewritten objects). |
| 2 | **StorageCredentials** (server↔storage, LOCKED 5b) | Not a standalone interface — a **responsibility boundary**: only `storage.New(cfg)` reads credentials. `cfg.Storage` is a tagged union: exactly one of `{S3, GCS}` non-nil (fail-fast). Nothing downstream sees a key. | aws default-chain (env / instance-profile / IRSA) \| static \| assume-role. Baseline = default chain. Minio dev = static + endpoint override. | **CONFIRMED baseline (§8): server-owned single identity** — ADC / Workload Identity on a GCE instance with an attached SA (no key on disk); `option.WithCredentialsFile` only for dev. Bucket-read scope. **Per-user impersonation is a documented v2 extension point, not v1.** |
| 3 | **ClientAuthenticator** (client↔server, LOCKED 5a) | `package authn`: `Verify(ctx,token,remoteAddr) (principal,err)`, called once per WS in the `Hello` handler; failure → `Error{AUTH_FAILED}`. v1 `NewBearerToken` constant-time compare. | `NewBearerToken(cfg.Auth.BearerToken)`, `principal="shared"`. No change. | Identical impl over `wss://`. Asensus's "auth via existing GCS credentials + IAM" is interpreted as **storage** auth (seam 2), not client auth. **Known v1 limitation:** under a single shared token every principal is `"shared"`, so the spec §8.2 "resume re-auth prevents hijack" guarantee is *vacuous* in v1; resume-hijack protection is deferred to the OIDC extension. Documented, not silently asserted. |
| 4 | **FormatCodec** (recording format) | `package format`: `Extract(ctx,bs,key) FileSummary` (indexer; reads embedded MCAP metadata into `tags_embedded`), `PlanChunks(summary,topics,tr) []ChunkRef` (per-file, fed into `session.BuildPlan`), `Iterate(chunk,ref,tr,emit)`. `NewCodec(kind)` — v1 only `"mcap"`. | `format/mcap` wraps `mcap-go`: footer/summary via small ranged reads; chunk-index intersection; chunk decompress + filtered iteration. The §5.2/§8.2 logic lifted behind the interface. | Identical MCAP impl. Payoff = documented extension path: a later format is a new impl + `NewCodec` case + a `format:` config key; engine untouched. One interface, one impl, **no plugin system**. |
| 5 | **BackendConnection** (client transport) | C++ `client-core`: promotes Mosaico's `unique_ptr<MosaicoClient>` member to an interface whose virtuals + result callbacks match `fetch_worker.hpp:74-101`. **See §3.3 for the file_ids[]-vs-sequence_name adapter — this seam is NOT a pure transport swap.** | `PjCloudConnection` wraps `client-core` (CloudConnection + MessageDispatcher + CatalogClient + SessionClient) over WS+Protobuf. | Byte-for-byte identical impl. GCS is server-side; the client sees no backend difference. "TLS in transit + compression" met by `wss://` + per-message ZSTD. |
| 6 | **TopicIngestDriver + SessionCache** (client ingest + in-memory reuse) | C++ `client-core`: `TopicIngestDriver` (begin/ingest/end onto Plan B `SessionSink`); `SessionCache` (`lookup(SessionKey)`, `beginStore`) — **in-memory only**. `SessionKey` hashes the **normalized** `(server_uri, file_ids[], topic_names[], time_range)` tuple. **See §3.3/§3.4: the connector does NOT decode (parsers do), and the cache has no disk tier.** | `RawMcapForwardingDriver` **forwards** each raw `(topic, schema_name/encoding, payload, log_time)` to the host — decoding is delegated to PlotJuggler's **MessageParser plugin family** (ROS/Protobuf/JSON-Schema), *not* implemented in the connector (§3.3). v1 CLI reuses the same path as `McapWriterSink`. Plugin (M2): `DatastoreSink` + in-memory `SessionCache`. | Same forwarding driver + same in-memory `SessionCache` (one impl, both engagements — keyed on logical selection, transport-agnostic). |

Smaller client seams: **AuthProvider** (bearer-token header + TLS config, persisted per-URI via `SettingsStore`; qtkeychain in the plugin, plain settings in the CLI) and **BackendCapabilities** (`HelloResponse{supports_file_hierarchy, metadata_key_vocabulary}` toggling an *additive* `QTreeWidget` browser mode).

### 3.3 Client — `client-core` + Mosaico-derived PJ4 toolbox dialog (with the two corrections)

`client-core` (Qt-aware, **Widgets-free** — QtCore/QtNetwork/QtWebSockets) requires **zero** change for either engagement: it speaks the storage-agnostic WS/Protobuf/MCAP wire. Built once, shared by (a) v1 `client-cli` (`McapWriterSink`, proves round-trip equality on both backends), and (b) the deferred PJ4 plugin (`DatastoreSink` + in-memory session reuse), and (c) the Dexory WASM demo (same `client-core` compiled to WASM).

**Genuinely reused unchanged from `toolbox_mosaico`** (verified against source): the Lua metadata query stack (Engine/Parser/AST/assist) — which directly delivers Asensus's metadata search by robot ID / procedure date / operator; `computeVisibleSequences`; name/date filters; `server_history` + URI normalization + per-server credential cache; `SettingsStore`; the worker-thread/command-queue/`onTick` async→GUI bridge; the three-column splitter UI; table sort + selection re-map; speed sampling; image ontology routing. Rename `mosaico_` → `cloud_connector_`.

**Correction A — the `file_ids[]` vs `sequence_name` selection-model adapter (this revises the "1:1 reuse" claim).** Mosaico's `SequenceRecord` is keyed by a single `std::string name` and `pullTopicsAsync(sequence_name, …)` does **per-topic parallel** pulls of **one** sequence. Our wire model opens **one** multiplexed session over a **set** of `file_ids[]` with server-side stitching. The headline Dexory case — "3+ consecutive recordings as one session" — is N `SequenceRecord`s in Mosaico's model but **one** `OpenFresh` on the wire. The `BackendConnection` seam therefore **must** include a defined adapter: **a multi-file selection is presented to the dialog as one synthetic stitched `SequenceRecord`** (time range = union, topics = union), and `pullTopicsAsync` on that synthetic record maps to a single `OpenFresh{file_ids[], topic_names[], time_range}`. This is dialog-selection-model logic, not a pure transport swap; it is an explicit M2b deliverable. (Where the data genuinely *is* 1:1: file→synthetic-sequence, flat `tags_effective`→`user_metadata`, time slider→`OpenFresh.time_range`, `Progress`/`Eos`→`pullProgress`/`pullFinished`/`allFetchesComplete`.)

**Correction B — the connector forwards raw records; it does NOT decode (this resolves the earlier "net-new decode" concern in the connector's favor).** **Decision (§8):** the cloud connector is a pure **data source**. It hands each raw MCAP message `(topic, schema_name/encoding, payload, log_time)` to PlotJuggler's existing **MessageParser plugin family** (ROS / Protobuf / JSON-Schema) via the host SDK; **decoding into numeric series is the parsers' job, explicitly out of the connector's scope** (proposal §3.1). So there is **no decode pipeline to build in M2b** — only a thin `SessionSink`/`DatastoreSink` that routes raw records into the host's message-parser dispatch. The remaining concrete unknown is small and bounded: confirm the exact PJ4 SDK entry point that accepts a raw `(schema, topic, payload)` message and routes it to the registered parsers (vs. the Arrow bulk path Mosaico used because its wire format already *was* Arrow). This shrinks M2b relative to the original estimate.

**Caveat on "DialogState compiles unchanged":** true for the filter/query/selection fields, but Mosaico's `topic_meta`/`topic_infos` are Arrow-schema-typed; the topic-detail panel data model is **not** zero-edit. Scope the "zero edits" claim to filter/query/selection only.

Dexory-vs-Asensus on the client is **one binary**: GCS-vs-S3 is invisible (storage auth is server-side); flat-vs-hierarchy is a `BackendCapabilities` flag toggling an additive tree view, not a rewrite.

### 3.4 The client cache — in-memory, session-lifetime reuse (aligns with the spec)

**No spec edit.** The design spec lists "Local on-disk caching of streamed sessions on the PJ4 client" as a v1 **non-goal** ("Memory-only via `pj_datastore`"; "no local disk cache"), and the review confirmed `pj_datastore` has only an in-memory store with retention-window eviction — **no disk tier**. **Decision (§8): the v1 cache is in-memory, session-lifetime reuse only.** This *matches* the spec, so — unlike an earlier draft of this plan — it is **not** a reversal and carries **no net-new disk-cache engineering.**

What v1 delivers and what it does not:
- **`SessionCache` is an in-memory map** keyed on the normalized `(server_uri, file_ids[], topic_names[], time_range)` tuple. Within one running PlotJuggler/CLI process, re-opening an identical selection is served from memory — **no second download, server-side session count == 0** on the repeat open.
- **Exact-tuple match only** (no subset/superset partial reuse — consistent with whole-selection bulk, LOCKED 2). **Only COMPLETE downloads are cached;** `Eos{CANCELLED|ERROR}` leaves no entry. Bounded by a memory budget with LRU eviction (no persistent metadata, no on-disk artifact).
- **Not persistent.** A PlotJuggler restart, or a different engineer, re-downloads. So Asensus's SOW line *"repeated access to the same time ranges does not require additional downloads"* is honored **only within a session**, not across restarts/users.

**Residual contractual item (§8):** if Asensus needs cross-restart / cross-user persistence, that is owned by their separate **"Task C" L3 disk cache** (the connector would *integrate* with it once it exists), or the SOW phrasing is confirmed/renegotiated. This plan does **not** build a disk cache.

### 3.5 Deployment — GCE confirmed; Cloud Run out for v1

**Baseline (both engagements, CONFIRMED §8): a single long-lived container / process.** Dexory = self-hosted single Go binary via systemd or Docker. Asensus = a long-lived container on **GCE** (Compute Engine VM or managed instance group), with an instance-attached service account for ADC (no key file) and a persistent disk for `catalog.db`.

**Constraint — Cloud Run is OUT for v1** (decision recorded; state it in the Asensus SOW). The server holds four pieces of in-process, in-memory state that Cloud Run's request-scoped, scale-to-zero, horizontally-autoscaled model breaks: (1) the session registry (`subscription_id → *Session`); (2) the bounded retain buffer (256 seqs / 64 MB) backing reconnect-resume; (3) the 60 s retain-after-disconnect window + producer/consumer goroutines that survive a WS drop; (4) the always-on background indexer poller (no request hosts it). Single-writer SQLite + in-memory sessions also do not scale horizontally, so **v1 is explicitly single-instance** (SQLite WAL → Postgres later if multi-instance is ever needed).

**Why GCE costs Asensus nothing on its stated deliverables:** "lazy-loading + prefetch" is met **client-side within a session** (selection bulk + in-memory reuse, §3.4), not by server-resident per-view state; cross-session persistence is a separate concern (Task C / wording, §3.4) and is not a server responsibility either way.

**Defer (scoped paid follow-on, only if Asensus later mandates Cloud Run):** a "stateless mode" persisting session state to Redis/Firestore + the indexer as a Cloud Scheduler job. A meaningful change, not v1.

> **Note for the deployment narrative:** keep "resume" (server-side retain, mid-stream WS drop) distinct from "fast re-open" (client in-memory reuse). The client cache must **not** be presented as a substitute for the spec's server-side resume guarantees; both exist and serve different purposes.

---

## 4. Repo / module layout — ONE repo, sibling to PJ4

One new repo `pj-cloud/`, sibling to `PJ4/` (not a subdirectory of the `mcap_server` design workspace). Go server and CMake/Qt client share `proto/pj_cloud.proto`; generated bindings are checked in so consumers need no `protoc`. **[REVISES Plan A]** Task 13 `s3reader` is promoted/renamed to `internal/storage` with the `BlobStore` interface extracted and a GCS sibling added; **[ADDS]** `internal/format` (one MCAP `FormatCodec`) and `internal/authn`; everything else keeps the spec §4 layout.

```
pj-cloud/                                      # NEW REPO, sibling to PJ4/ (PRIVATE; Dexory team granted access — proposal line 112)
├── proto/pj_cloud.proto                       # canonical wire schema (Go + C++ bindings checked in); ZERO storage fields
├── server/                                    # Go server (own go.mod)
│   ├── cmd/pj-cloud-server/main.go
│   ├── internal/
│   │   ├── config/                            # YAML+env; storage = s3 XOR gcs tagged union
│   │   ├── storage/                           # SEAM 1+2: BlobStore + factory (StorageCredentials boundary)
│   │   │   ├── blobstore.go  factory.go  reader.go(UNCHANGED adapter)  s3reader.go  gcsreader.go
│   │   ├── format/{codec.go, mcap/}           # SEAM 4: FormatCodec + one MCAP impl
│   │   ├── authn/                             # SEAM 3: ClientAuthenticator + NewBearerToken
│   │   ├── catalog/ indexer/ session/ wire/ ws/ dashboard/ metrics/
│   ├── go.mod
│   └── deploy/
│       ├── Dockerfile  config.example.yaml(BOTH s3+gcs blocks)
│       ├── dexory/    # systemd + docker-compose (self-hosted, long-lived)
│       └── asensus/   # GCE deploy guide: attached SA/Workload-Identity, TLS, persistent disk, infra reqs
├── client-core/                               # Qt C++ static lib (Widgets-free) — ZERO per-engagement change
├── client-cli/                                # QCoreApplication + McapWriterSink
├── integration-tests/                         # Plan C: docker-compose(server+Minio+fake-gcs), fixtures, fake-server, bench, round-trip
├── CMakeLists.txt  conanfile.txt  README.md  LICENSE
└── # DEFERRED (Dexory M2 / Asensus client deliverable): PJ4/pj_plugins/pj_cloud/ lifts client-core,
  #   adds DatastoreSink (raw→MessageParser dispatch) + in-memory SessionCache, the Mosaico-derived
  #   CloudOpenDialog, qtkeychain, pjcloud://
```

---

## 5. Concrete milestones

Shape = shared-core milestones (M0, M1a, M1c, M2a, M2b) + thin per-client adapter milestones (M1b GCS, M2c-DEX, M2c-ASEN). Sequencing follows spec §13 (1a proto → 1b server → 2 client-core → 3 round-trip → 4 deferred plugin), refactored into a kill-two-birds shape.

> **Effort vs calendar (must be reconciled with the user — see §8).** The proposal commits ~1 month per Dexory milestone. The effort estimates below sum to more than that. Two honest options: (i) the per-month commitment is a *calendar* commitment for *Dexory-funded scope only* and the effort estimates are wall-clock-with-slack to be compressed/parallelized; or (ii) the proposal's durations need revising. The plan must not silently claim both. **The S3-only Dexory M1 path (M0 + M1a-S3 + M1c) is what must fit Dexory's month; M1b (GCS) is Asensus-funded and must not be billed into Dexory's fixed-price M1.** (The 2026-06-03 decisions trimmed M2b — in-memory cache instead of a disk cache, and forward-raw instead of in-connector decode — which eases, but does not erase, the overrun.)

> **M1 contract-scope reconciliation (must-fix, see §8).** The proposal (lines 119-120) lists **tag editing** and **intra-file time-range selection** as **OUT of Dexory M1** ("M1 supports whole-file topic filtering; M2 adds intra-file time windows"). **This plan's default is to honor the contract:** `UpdateTags` and intra-file time-range slicing move to **M2** for the Dexory engagement. The engine code can still be *built* early (it is cheap and Asensus may want time-range in its own timeline), but the **Dexory M1 acceptance gate must test whole-file topic filtering only**, and the M1c round-trip matrix's time-range dimension is exercised under Asensus/internal scope, not as a Dexory M1 gate — **unless** Dexory agrees in writing to an expanded M1.

| ID | Goal | Key deliverables | Acceptance gates | Dexory map | Asensus map | Depends |
|---|---|---|---|---|---|---|
| **M0** (~1.5 wk) | Pin the expensive-to-change shared surface: wire schema, six seam interfaces, storage tagged-union config, CI skeleton with a live `{s3,gcs}` matrix axis. Spec Phase 1a + seam scaffolding. **The pulled-forward Asensus items are RESOLVED (§8): server-owned SA for GCS; MCAP-footer metadata** — so no object-metadata path is built. | Repo skeleton (private; Dexory access provisioned). `proto/pj_cloud.proto` (full envelope; `BackendCapabilities`; `FileFilter` = TimeRange + topics_any_of + TagPredicate any/all + page_token/limit; ZERO storage fields). Flat-metadata SHAPE REQ verified vs `mosaico_dialog.hpp`. Go+C++ codegen, bindings checked in. Seam interfaces with v1 impls: `storage.BlobStore`+factory stub, `format.FormatCodec`+`NewCodec`(MCAP deferred to M1a), `authn.NewBearerToken`. Config tagged-union (s3 XOR gcs, fail-fast); `storage_concurrent_requests`; `format:` defaults `mcap`. `config.example.yaml` (both blocks). CI: Go unit+lint+race, C++ unit+clang-tidy, integration job with `{s3,gcs}` axis (gcs leg placeholder until M1b). | `make proto` deterministic (CI fails on drift). Envelope encode↔decode round-trip passes in **both** Go and C++ on a shared golden corpus. Config: both/neither blocks fail fast; valid s3-only and gcs-only each load. Compile-time assert: `ListFilesResponse` carries flat `string→string` metadata; fixture maps onto Mosaico `user_metadata` without transform. `NewBearerToken` correct→principal, wrong→AUTH_FAILED, constant-time. CI green on all jobs. | M1 foundation | Wire protocol; storage abstraction seam; auth seam separation; catalog metadata shape | — |
| **M1a** (~4 wk) | Build the ~70% shared Go server core behind `BlobStore`+`FormatCodec`; wire the **S3** impl + **MCAP** codec first so Dexory M1 ships. Spec Phase 1b. | catalog (SQLite-WAL, `tags_effective`, single-writer, ListFiles predicates+pagination, GetFile, UpdateTags). **[Dexory scope note: `UpdateTags` engine exists but is NOT a Dexory M1 gate — see scope reconciliation.]** `format/mcap` (Extract incl. footer-metadata→`tags_embedded`; PlanChunks; Iterate). `storage/s3reader.go` (BlobStore over aws-sdk-go-v2; error normalization 503/429/timeout→Transient(50→800ms ×5), 403/404/NoSuchBucket→Permanent; semaphore; Minio endpoint). indexer (poll, change-detect triple, Extract, override-preserving re-index, failures on dashboard; **footer metadata only — no Head-custom-metadata read**). session (BuildPlan incl. **multi-file stitch+overlap-rejection above the codec**, pre-flight estimates, empty-plan contract; retain 256/64MB; producer; consumer + SessionAck; registry + 60s eviction; OpenResume; CancelSession). ws (dispatcher, Hello, handlers, per-goroutine recover + counters). dashboard+metrics (HTML, Basic auth, /health, /metrics, backend display line). `cmd/pj-cloud-server` (pure-Go). Component-integration suite vs Minio (spec §11 Layer 2). | Server boots from S3(Minio), indexes a fixture bucket, serves catalog + a full streaming session over the real wire. Component-integration passes incl. multi-file stitch, empty-plan contract (`{0,0}`+immediate `Eos COMPLETE`), overlap rejection (`INVALID_REQUEST`), cancel (`Eos CANCELLED`), resume success + resume-after-eviction (`RESUME_NOT_POSSIBLE`). S3 transient→backoff→success; 403→`S3_UNAVAILABLE`+`Eos{ERROR}`. Concurrency cap→`RESOURCE_LIMIT`; no goroutine leak. Dashboard Basic-auth; /health unauth; /metrics. `go test -race` green. ListFiles returns flat `tags_effective` (SHAPE REQ). | M1 — server v0.1 core | Cloud connection server core; catalog metadata+time+topic filtering; eager-bulk streaming; **compression in transit (per-message ZSTD)**; metadata search fields from MCAP footer; dashboard+/metrics+/health | M0 |
| **M1b** (~1.5 wk, **Asensus-funded**) | GCS `BlobStore` drop-in behind the SAME interface in the SAME Phase-1b tier; run the entire component suite + storage-parity microbench on **both** backends. The single most important anti-drift call. Built after S3 (so Dexory M1 ships first) but **inside** the M1 window, NOT deferred. | `storage/gcsreader.go` (NewRangeReader/Attrs/Objects+Pager; ETag-mapping pin from M0; same error classes; same semaphore). StorageCredentials GCS branch (**server-owned**: ADC/Workload-Identity baseline, `WithCredentialsFile` for dev; bucket-read scope). `fake-gcs-server` docker-compose job mirroring Minio. Same fixture/component matrix parameterized over `{s3,gcs}`. `bench/` storage-parity microbench. CI gcs leg activated. | **Entire M1a component suite passes unchanged against fake-gcs-server** (same assertions, only config differs). Zero backend-specific branching in shared packages (grep/lint: only `internal/storage` imports a cloud SDK). Storage-parity microbench: GCS within ~10% of S3, recorded as baseline. `{s3,gcs}` matrix green on both legs. | M1 — no Dexory-facing change; guards against a future fork | GCS backend (config-selectable, Range GETs); GCS auth (server-owned IAM/Workload-Identity) | M1a (GCS access + metadata source already resolved in §8) |
| **M1c** (~3 wk) | Qt `client-core` + `client-cli` + `McapWriterSink`; prove lossless round-trip on **both** backends. The headline Dexory M1 deliverable. Spec Phases 2+3. | client-core (Widgets-free: CloudConnection, MessageDispatcher, CatalogClient, SessionClient, SessionSink). client-cli (`files list/show`, `session download/debug`; **`tag` subcommand exists but is NOT a Dexory M1 gate per scope reconciliation**; `--json`/`--progress`; token via flag/env). client-core unit tests (envelope vs Go, dispatcher routing/out-of-order, SessionClient state machine, Decompression ZSTD/LZ4 + truncated-fails-clean, SessionKey normalization). client-core component vs fake-server (lifecycle, decompression dispatch, cancel, resume-with-drop, malformed-envelope rejection). Deterministic MCAP fixture matrix. E2E round-trip harness (spec §11 Layer 3) on Minio AND fake-gcs. Live-demo runbook + 8–12 page M1 report. | **DEXORY M1 GATE:** server runs against a **real S3 bucket**, indexes **≥100 real MCAPs**; CLI opens a session over **≥3 consecutive recordings**, downloads only requested topics (**whole-file topic filtering**), writes an MCAP with logical equality on `(topic, log_time, payload, publish_time, schema name/encoding/data)`. E2E matrix passes on **both** `{s3,gcs}` (Asensus inherits the gcs leg); `estimated_chunk_bytes` within 5% of actual. client-core unit+component green; links with **no Qt6::Widgets** (build-graph check). Repeatable live demo. 8–12 page report delivered. **COMMERCIAL GATE: explicit written Dexory M1 approval recorded before any M2 work begins.** *(Intra-file time-range cases are validated internally/for Asensus, not as a Dexory M1 gate, unless Dexory agrees to expanded M1.)* | M1 — reference client + round-trip + demo + report + acceptance gate | MCAP round-trip foundation (shared client-core); reference client proving the wire path | M1a, M1b |
| **M2a** (~3 wk) | Production hardening for both engagements + shared deploy artifacts + throughput gate. **Gated by Dexory M1 written approval** (start of Dexory M2). **For the Asensus engagement, M2a is funded/triggered by Asensus independently of the Dexory gate — see §8.** | Hardening: panic recovery + counters on every goroutine boundary; graceful shutdown; transient-retry under load (S3+GCS); write timeouts; slow-client backpressure (no leak, RSS bounded, Progress p99<200ms). Dashboard polish over TLS+Basic auth. Shared deploy artifacts (distroless Dockerfile, docker-compose, config reference). V1 benchmark suite (spec §11 Layer 4) as CI gate. Cross-language E2E matrix as per-push/nightly gate on `{s3,gcs}`. Operator docs. | Throughput **≥200 MB/s sustained on the defined reference machine** (CPU/NIC/disk pinned in the SOW — see §8); >25% regression fails CI. Backpressure: goroutines flat, RSS bounded, Progress p99<200ms. Graceful shutdown drains/cancels cleanly; no orphaned goroutines. E2E matrix green on both backends; ≥1-week soak before M2 sign-off. docker-compose + systemd each reach healthy /health from clean config. | M2 — dashboard, deploy artifacts, hardening, E2E+throughput gate, operator docs | **TLS in transit** (wss+server.tls); deploy-artifact foundation; production dashboard+/metrics+/health | M1c |
| **M2b** (~2.5 wk) | Lift `client-core` into a PJ4 DataSource plugin by reusing the Mosaico dialog and swapping the seams. SAME work for Dexory M2 and the Asensus client deliverable. Spec Phase 4. **(See §3.3/§3.4: ingest is a thin forward-to-MessageParser sink — no in-connector decode — and the cache is in-memory — no disk cache. Trimmed vs the earlier estimate.)** | `BackendConnection` impl (`PjCloudConnection`) **incl. the file_ids[]→synthetic-stitched-SequenceRecord adapter (§3.3)**. `TopicIngestDriver` (`RawMcapForwardingDriver`) that **forwards raw `(topic,schema,payload)` to the host MessageParser dispatch (§3.3)** — decoding is the parsers' job, not the connector's; reuses the `host_write_mu_` region. `SessionCache` **in-memory, exact-tuple, LRU (§3.4)** — no disk tier. `AuthProvider` (Bearer-over-wss + qtkeychain). PJ4 packaging (`pjcloud://`, CloudOpenDialog, ProgressDialog, `launchCustomOpenDialog()` hook). Tag editing UI via `UpdateTags`. Reconnect-resume via the plugin. End-user docs. | **DEXORY M2 GATE:** plugin installs into stock PlotJuggler v4, opens a cloud session in ~5 clicks; series appear via the **existing MessageParser plugins** (connector ships no decoders). Non-author engineer completes an E2E investigation from docs alone. Tag edits persist across re-index. Reconnect-resume: simulated drop resumes from last ack, no refetch, no dupes. **In-memory cache hit:** within one running session, re-opening identical `(files,topics,time-range)` serves from memory with **server-side session count == 0** (no cross-restart persistence is required or claimed). `client-core` reused with only the seams swapped (build-graph: no client-core edits). | M2 — connector plugin, tag editing, reconnect-resume, end-user docs | PlotJuggler cloud plugin; metadata search (robot/date/operator) via Lua query-assist; lazy-load via selection-bulk + in-memory reuse (within-session); raw-message forwarding to host parsers | M2a |
| **M2c-DEX** (~1 wk) | Dexory-facing adapter + WASM bonus. No new engine work. | `deploy/dexory/` (systemd + docker-compose vs Dexory S3; runbook reviewed by Dexory ops). Dexory auth config (shared bearer + aws default-chain). WASM bonus: client-core→WASM running the PJ4 WASM UI against the unmodified wire; URL-shareable selection; **a CI job that at minimum COMPILES client-core to WASM so the path cannot silently rot** (see §6/§7). M2 demo script incl. browser preview. | Self-hosted deploy reaches healthy /health vs Dexory's real S3; runbook reviewed by Dexory ops. **DEXORY M2 GATE (full):** E2E green ≥1 week, 200 MB/s holds (from M2a), 5-click session (from M2b), ops review complete. **WASM is a DEMO deliverable, NOT a pass/fail acceptance gate** (per proposal §6 "credible preview, no Dexory-side cost") — a WASM/browser-toolchain regression must not block paid-core M2 sign-off. | M2 — Dexory deploy artifacts + runbook + WASM bonus + acceptance gate | — | M2a, M2b |
| **M2c-ASEN** (~1.5 wk) | Asensus-facing adapter: GCE guide + IAM + additive file-hierarchy browser. No new engine work beyond the additive browser mode (metadata already lands from the MCAP footer in M1a). **Includes the explicit Asensus acceptance gate.** | `deploy/asensus/` (GCE guide + infra reqs: long-lived container, attached SA/ADC no-key, persistent disk for catalog.db, TLS/wss; explicit "Cloud Run OUT for v1" with the four state reasons and the why-it's-safe note; **capacity-planning guidance: size the instance against `session.max_concurrent`, default 16**). `BackendCapabilities`-driven additive `FILE_HIERARCHY` QTreeWidget over GCS prefixes (fed by the SAME ListFiles; breadcrumb; resolves to file_ids→existing fetch flow). Server-owned GCS access model documented; per-user impersonation as v2. | **ASENSUS ACCEPTANCE GATE (the Asensus engagement done-state):** long-lived container on a GCE VM with attached SA reads the bucket via ADC (no key on disk) and serves catalog + a streaming session over `wss://` to healthy /health; **GCS round-trip E2E green (the `{gcs}` Layer-3 leg)**; file-hierarchy browser works (flag true→tree; flag false→Dexory flat table unchanged); **metadata search by robot_id/procedure_date/operator works** from the MCAP footer, assist dropdowns populate from `metadata_key_vocabulary`; **TLS in transit confirmed (wss end-to-end)** and **compression in transit confirmed** (per-message ZSTD, evidenced by the Layer-4 compression-CPU bench); deployment guide + infra reqs delivered; the residual SOW caching-wording item (§8) confirmed in writing. **Real-GCE ADC validation runs on a scheduled GCE self-hosted runner or a documented manual checklist (cannot run in vanilla CI).** | — (Asensus-specific) | GCS file browser; metadata search; GCE deploy guide + infra + IAM; GCS auth model (per-user = v2); MCAP-first + documented format extension path; **TLS + compression confirmed** | M2a, M2b, M1b |

**Commercial framing.** M0 + M1a-S3 + M1c = **Dexory Milestone 1** (PoC, EUR 10k); written approval hard-gates M2. M2a + M2b + M2c-DEX = **Dexory Milestone 2** (EUR 6k). **M1b + the Asensus-funded triggers of M2a/M2b + M2c-ASEN = the Asensus-incremental work**, consistent with the ~8–12% thesis. **30 days of post-acceptance bugfix support apply after each Dexory milestone (proposal §8 line 185)** — acknowledged here as a post-M1 and post-M2 support window.

---

## 6. Testing strategy

Layered matrix; every layer ties to a milestone gate. Cross-backend by construction.

| Layer | Scope | Gate | Milestone tie-in |
|---|---|---|---|
| **L1 — Unit** (per-language, hermetic, no live deps) | Go: catalog SQL + `tags_effective` + pagination; indexer change-detect on the triple (incl. GCS Generation/Updated mapped in); `format/mcap` Extract (incl. footer-metadata→`tags_embedded`)/PlanChunks/Iterate vs fixtures; `session.BuildPlan` (intersection, estimates, empty-plan, **multi-file stitch+overlap rejection**); BlobStore **error-normalization per impl** against canned SDK error values (no network) — asserts **both** impls collapse to the same Transient/Permanent classes; semaphore wrapper; authn constant-time; config tagged-union. C++ (QTest): envelope encode↔decode on a **shared golden corpus run in both Go and C++**; dispatcher correlation/out-of-order/routing; SessionClient state machine; Decompression + truncated-fails-clean; **SessionKey normalization+hash** (reordered file_ids/topics → same key; different uri → different key). | **Blocks every push.** Go <30s, C++ <2min incl. clang-tidy; race job green. | M0/M1a/M1c. Shared-corpus + SessionKey tests are the cheapest insurance against proto drift and cache-key bugs. GCS error-map units land in the SAME Phase-1b tier as S3. |
| **L2 — Component** (single language, LIVE local storage) | Server (Go) **parameterized by backend, run twice via `t.Run(backend)`** against Minio AND fake-gcs-server: catalog filters; tag-override flow; indexer warm+incremental (override survives re-index); full session lifecycle (single/multi-file stitch/topic-subset/time-subset/compressed); cancel; resume (success in window + `RESUME_NOT_POSSIBLE` after eviction); transient (503/429)+permanent (403/404) injection; slow-consumer backpressure; concurrency cap; dashboard Basic-auth; /health+/metrics unauth. Client-core (C++) vs fake-server: lifecycle, per-encoding decompression, cancel, resume-with-drop, malformed-envelope rejection. **In-memory cache component tests:** HIT within a running session avoids refetch (transport-call counter == 0); key/range correctness (narrower time-range or different topic set → MISS); LRU eviction by memory budget → re-fetch; **only COMPLETE downloads cached** (cancelled/errored → no entry); cache reuse yields a reconstruction **byte-identical across S3 vs GCS** (proves backend-agnostic). *(No cross-restart persistence test — the cache is in-memory by decision, §3.4.)* | **Blocks every push** (server-component ~2min, client-component ~1min). **Backend-parameterized server suite MUST be green on BOTH Minio and fake-gcs-server — a GCS-only failure blocks the merge exactly like an S3-only failure** (operational expression of the no-soft-fork rule). | Dexory M1 (Minio = release-gating); fake-gcs leg in the same tier so Asensus inherits a proven GCS path. In-memory-cache + eviction tests are the concrete proof of the within-session reuse deliverable. |
| **L3 — E2E cross-language round-trip** (REAL Go server + REAL C++ CLI + REAL local store) | Boot docker-compose (server + Minio + fake-gcs), upload deterministic fixtures, `pjcloud-cli session download`, read back with mcap-go, assert **logical equality** on `(topic, log_time, payload, publish_time, schema name/encoding/data)` — **not** container-byte equality (writers legitimately differ on chunking/summary-ordering/compression; **this matches design-spec §11 line 833, which already specifies logical equality**). Matrix: files{1,3,10} × topics{all,half,single,none} × time-range{full,mid30,boundary,outside} × concurrency{seq,4-parallel} (~32) + edge fixtures (large→oversized singleton batch; tiny→batch boundaries; embedded-tags; consecutive-1/2/3of3 w/ schema drift on file 3; empty-time-range→COMPLETE not error; corrupt-chunk→error path). Assert `estimated_chunk_bytes` within 5% of actual; exit codes. | **THE v1 release gate.** Per-push to main + nightly on branches; the `{s3,gcs}` variant is the **merge gate for anything touching `storage/`, `format/`, `session/`, `ws/`, or `proto/`**; any logical-equality mismatch on **any** backend blocks release. <5min/leg. | This IS the Dexory M1 acceptance criterion (whole-file topic filtering for the Dexory gate; time-range dimension internal/Asensus per §5 scope note). The identical matrix on the `{gcs}` leg is the Asensus round-trip acceptance proof. Spec §13 Phase 3 / Plan C. |
| **L4 — V1 benchmark gate** (small but real; regression-guard, NOT an SLO) | (1) Throughput (server+store same host, 1GB, 4KB RAW) **≥200 MB/s on the DEFINED reference machine** (CPU/NIC/disk pinned — §8); flag >25%. (2) ZSTD CPU ≥80 MB/s, CPU not pegged. (3) Slow-consumer (1MB/s throttle): goroutines flat, Progress p99<200ms, RSS bounded. (4) **Cross-backend storage-parity microbench:** BlobStore.GetRange on fake-gcs within ~10% of Minio. Each writes JSON compared vs committed `baseline.json`; sub-baseline fails; baseline moved only by explicit PR. | **Nightly + on-tag** (not every push). `compare.go` non-zero → fail the tag. **Storage-parity is a HARD gate the first time GCS lands** (locks the abstraction), a soft regression-flag thereafter. | Proposal lists benchmark targets as v1 gates. **[REVISES framing vs design-spec §2/§11:]** the spec frames throughput numbers as *aspirational/non-SLO*; the proposal (line 152) makes 200 MB/s an M2 acceptance criterion. We honor the proposal as a **regression gate against a pinned reference machine**, not an open-ended SLO. The parity leg is the quantitative half of the no-soft-fork guarantee. |

**Auth tests (both seams, LOCKED 5):** client↔server (correct bearer→principal; wrong/empty/malformed→`AUTH_FAILED`, WS closed; constant-time); bearer-over-wss handshake + CLI `--insecure` self-signed path; protocol-version mismatch→`PROTOCOL_VERSION`; **resume re-auth documented as a v1 no-op under shared bearer** (every principal `"shared"` — hijack protection deferred to OIDC). Server↔storage S3 (default-chain/static/assume-role; wrong secret→403→Permanent→`S3_UNAVAILABLE` with **no creds leaked**; NoSuchBucket→Permanent). Server↔storage GCS (ADC/Workload-Identity faked metadata, `WithCredentialsFile` for dev; invalid/expired creds fail fast; 403→Permanent; ErrObjectNotExist→Permanent). **Seam-separation:** valid client token + bad storage creds → Hello OK then `S3_UNAVAILABLE` at fetch; valid storage + bad token → `AUTH_FAILED` before any storage access. Config fail-fast on both/neither. **No-credential-leak regex scan** of `Error.message/details`. *(The "signed-URL secret" leak case from the draft test list is dropped — the BlobStore impls use SDK-managed credentialed clients, not signed URLs.)*

**Resume/cancel tests:** resume success within window (final reconstruction byte-equal to a no-drop download); resume after eviction→`RESUME_NOT_POSSIBLE`→client falls back to `OpenFresh`; producer-blocked-then-drain; server-restart→resume fails (in-memory retain lost)→`OpenFresh`; SessionAck pruning unblocks producer; cancel mid-stream→`Eos{CANCELLED}`, retain discarded, **no cache entry written**; cancel/MarkProducerDone race→exactly one terminal Eos; client reconnect loop (3 attempts, [1s,4s,16s]) then abort with sink ended on error. Resume-success and cancel run on **both** S3 and GCS legs (in-flight Range-GET cancellation must behave identically for aws-sdk and gcs `NewRangeReader`).

**Deployment smoke:** self-hosted (Dexory: docker/systemd → /health 200, dashboard over TLS+Basic, indexer warm-scan, CLI round-trip); **GCE (Asensus: long-lived VM/MIG, attached SA via metadata server, persistent disk catalog.db survives VM restart without full re-scan, wss round-trip)** — runs on a scheduled GCE self-hosted runner or documented manual checklist; config smoke (both example blocks; tagged-union fail-fast); **Cloud Run NEGATIVE/documented-constraint note** (why it's excluded; entry point for the scoped paid stateless-mode follow-on if confirmed); shutdown smoke (SIGTERM graceful, WAL checkpointed, clean warm restart); TLS smoke.

**WASM:** the only automated coverage is the M2c-DEX **compile-to-WASM CI job**; functional verification is the manual demo. Flagged explicitly: the WASM functional path is otherwise unverified in CI and could rot between demos.

**CI strategy.** Two workflows. **`ci.yml`** — per-push (blocks merge): Go unit (~30s) + Go race (~60s) + C++ QTest+clang-tidy (~2min) + server component on **both** Minio and fake-gcs via `t.Run(backend)` (~2min) + client-core component incl. in-memory-cache+eviction (~1min); shared envelope golden corpus in both Go and C++ jobs; lint/vet/gofumpt/clang-format. **Merge-to-main / PR gate:** L3 cross-language E2E as a `{s3,gcs}` matrix (~5min/leg); anything touching `storage/`/`format/`/`session/`/`ws/`/`proto/` requires both legs green; GCS-only failure blocks like S3-only. **`bench.yml`** — nightly cron + workflow_dispatch + on-tag: L4 throughput/compression/backpressure + storage-parity vs `baseline.json`. **Manual/pre-release (out of automated scope per spec §11):** real AWS S3 + real GCS smoke vs emulators (note: fake-gcs/Minio fidelity is itself unproven, so "GCS is a proven drop-in" is only fully true after ≥1 real-GCS smoke), multi-day soak, envelope fuzz (post-v1), real-GCE ADC deployment smoke. Mechanics: GitHub Actions; Conan 2 + CMake (cached) for C++; Go module + docker-layer caches; gotestsum + CTest. **Build S3 first to keep Dexory M1 on schedule, but wire the GCS leg into the same matrix from the day the BlobStore seam lands — GCS is a drop-in, never a retrofit.**

---

## 7. Risks & mitigations

| # | Risk | Severity | Mitigation |
|---|---|---|---|
| 1 | **Backend drift** — GCS lags S3 until backend-specific assumptions (error shapes, listing semantics, range-read behaviour) leak into shared code, eroding "one codebase" into a soft fork. | High | Both `BlobStore` impls stand up behind the interface in the **same Phase-1b tier** (M1b); the entire L2 component suite + storage-parity microbench run on **both** from day one; a **GCS-only CI failure blocks merges exactly like S3-only**; a grep/lint check forbids cloud-SDK imports outside `internal/storage`. |
| 2 | **Asensus "no re-download" wording vs in-memory-only cache** — the v1 cache reuses within a running session only; across PlotJuggler restarts or across engineers, re-download happens, so the literal SOW phrasing is not met cross-session. | Medium | **Decision (§8): in-memory only, which matches the spec — no disk cache is built.** Cross-session persistence is owned by Asensus's separate **"Task C" disk cache** (the connector integrates with it once it exists) or the SOW line is confirmed/renegotiated in writing (M2c-ASEN gate). Contractual, not engineering, risk. |
| 3 | **Mosaico reuse overstated** — file_ids[]-vs-sequence_name model mismatch; "reused unchanged" would break the 3-consecutive-files case. | Medium-High | The selection-model **adapter** (synthetic stitched SequenceRecord) is an explicit M2b deliverable with an effort line (§3.3); the "zero-edit" claim is scoped to filter/query/selection fields only. **Ingest is de-risked by decision:** the connector forwards raw records to the host MessageParser plugins (no in-connector decode, §3.3) — the only residual is confirming the exact PJ4 SDK "push raw message → parser dispatch" entry point. |
| 4 | **Commercial coupling** — Asensus's core client deliverables (plugin, in-memory reuse, metadata search) live in shared M2a/M2b, which are gated on **Dexory's** M1 written approval. If Dexory declines M2, Asensus's deliverables have no defined path. | High | **Decouple funding/trigger:** define an **Asensus-funded trigger for M2a/M2b independent of the Dexory M2 gate** (§8). Schedule keeps the two engagements' commercial gates separate even though the code is shared. M1b is Asensus-funded and must not be billed into Dexory's fixed-price M1. |
| 5 | **Timeline overrun vs priced month** — effort estimates exceed the proposal's ~1 month/milestone (eased but not erased by the M2b trim). | High | Reconcile calendar vs effort explicitly (§5 note + §8): the per-month commitment covers **Dexory-funded scope only**; Asensus-incremental effort (M1b, parts of M2) is separately funded; compress via parallelism (M2b ∥ M2c-DEX after M2a). If the month is genuinely binding for Dexory scope, trim Dexory M1 to the contract (whole-file filtering, no tag editing). |
| 6 | **M1 scope creep vs signed contract** — tag editing + intra-file time windows are M1-out-of-scope in the proposal. | Medium-High | Default to the contract: move both to M2 for the Dexory engagement; Dexory M1 gate tests whole-file topic filtering only; expanded M1 only on written Dexory agreement (§8). |
| 7 | **"byte-for-byte" wording** — proposal line 127 says byte-for-byte; we implement logical equality. | Medium | The **design spec §11 already specifies logical equality** as the test; confirm in writing that Dexory accepts message-level logical equality as satisfying the M1 gate (§8). Low technical risk, real contractual risk. |
| 8 | **WASM over-promised** — asserted "unmodified client-core to WASM"; QWebSocket-under-WASM can't present client TLS certs, the `--insecure` path doesn't translate, and ZSTD/LZ4 decode is main-thread CPU unless workerized. | Medium | Keep WASM a **demo (not gating) deliverable**; add a compile-to-WASM CI job; run a transport/TLS/decode spike before the demo; surface the constraints rather than asserting "unmodified." |
| 9 | **Emulator fidelity** — fake-gcs-server/Minio may not match real GCS/S3 (range/generation/error shapes); "proven drop-in" overstates emulator-only passing. | Medium | At least one **real-GCS and real-S3 smoke** before declaring drop-in; documented as manual/pre-release. |
| 10 | **Single-instance capacity** — Asensus may size a GCE instance for more concurrent reviewers than `session.max_concurrent` (default 16). | Low-Medium | M2c-ASEN deploy guide includes **capacity-planning guidance** keyed to `max_concurrent`; `RESOURCE_LIMIT` behaviour is tested. |
| 11 | **Silent spec edits** — config tagged-union/rename touch the spec's frozen §8.6 surface. | Low | All spec edits (§5.1 comment, §5.2 reader, §8.2/§8.3 retype, §8.6 tagged-union, `storage_concurrent_requests` rename) recorded as explicit reviewed amendments, not silent changes. The client cache is **not** among these — in-memory reuse matches the spec's existing non-goal. |

---

## 8. Open items

### Resolved (decided 2026-06-03)

These were the technical open items; they are now fixed and reflected throughout this document:

- **A. Client cache → in-memory, session-lifetime reuse only.** No disk tier; matches design-spec §2/§12 (no reversal); no net-new disk-cache work. Cross-restart/cross-user persistence is out of v1 (→ residual item below). (§2.3, §3.4)
- **B. Asensus deployment → GCE long-lived container; Cloud Run OUT for v1** (paid stateless-mode follow-on only if later mandated). (§3.5)
- **C. GCS access → server-owned single service-account / Workload Identity** (ADC on a GCE instance, no key on disk); per-user impersonation is a documented v2 extension. (§3.2 seam 2)
- **D. Metadata source → MCAP footer** (`FormatCodec.Extract`→`tags_embedded`); no object-custom-metadata read, no `ObjectInfo.Metadata` field, no `BlobStore.Head` metadata path. (§3.1 indexer)
- **E. Client ingest → forward raw records to PlotJuggler's MessageParser plugins; the connector bundles no decoders.** No decode pipeline in M2b — only a thin forward-to-parser sink. (§3.3 Correction B)
- **F. Multi-format depth → one `FormatCodec` interface + one MCAP impl + a documented extension path; no plugin system.** (§3.2 seam 4)

### Still open (mostly commercial — need Dexory/Asensus, not just an engineering call)

1. **Dexory M1 scope vs contract (gates M1a/M1c).** The proposal puts **tag editing** and **intra-file time-range selection** OUT of M1. Default plan: move both to M2 for Dexory; M1 gate = whole-file topic filtering only. **Honor the contract as written, or expand Dexory M1 (requires written Dexory agreement and possibly re-pricing)?**
2. **"byte-for-byte" acceptance (gates the M1 sign-off).** The proposal's M1 line says "byte-for-byte"; we implement **logical equality** on `(topic, log_time, payload, publish_time, schema)` (which is what the design spec §11 itself specifies). **Can we get Dexory's written acceptance that message-level logical equality satisfies the M1 gate?**
3. **Timeline vs price (gates the whole schedule).** Effort estimates still exceed ~1 month/milestone after the M2b trim. **Is "1 month/milestone" a calendar commitment for Dexory-funded scope only (with Asensus-incremental work separately funded), or do the durations / price need revising?**
4. **Asensus engagement boundary + funding decoupling (gates M2a/M2b for Asensus).** Asensus's core deliverables live in shared M2a/M2b, currently gated on Dexory's M1 approval. **How are M2a/M2b funded/triggered for Asensus if the Dexory M2 gate does not fire? Confirm the M2c-ASEN acceptance gate (§5) as the Asensus engagement done-state.**
5. **Residual cache-wording item (gates M2c-ASEN sign-off).** Given the in-memory-only cache (resolved item A), Asensus's "repeated access … no additional downloads" holds only within a session. **Confirm in writing that cross-restart/cross-user persistence is owned by Asensus's separate "Task C" disk cache (and that this connector merely integrates with it later), or renegotiate the SOW phrasing.**
6. **Reference machine for the 200 MB/s gate (gates M2a acceptance).** **Pin the CPU / NIC / disk in the SOW** so the throughput gate is enforceable. *Proposed default (for confirmation): a standard GCE `c3-standard-8` (or Dexory-equivalent 8-vCPU, ≥16 GiB, gVNIC ≥16 Gbps, balanced PD) with server and store co-located — adjust to whatever Asensus/Dexory will actually run.*
